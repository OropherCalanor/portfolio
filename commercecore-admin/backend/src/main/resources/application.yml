spring:
  application:
    name: commercecore-backend
  profiles:
    default: dev

server:
  port: ${APP_PORT:8090}

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5175,http://127.0.0.1:5175}

springdoc:
  swagger-ui:
    path: /swagger-ui.html
